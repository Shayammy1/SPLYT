<UserControl x:Class="NovaVM.Gui.Views.GpuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:NovaVM.Gui.ViewModels"
             xmlns:models="clr-namespace:NovaVM.Gui.Models"
             xmlns:markup="clr-namespace:NovaVM.Gui.Markup"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:GpuViewModel}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32">
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="GPU" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" />
                    <TextBlock Text="{markup:Tr Gpu_Subtitle}" FontSize="13" Margin="0,4,0,0"
                               Foreground="{StaticResource TextSecondaryBrush}" />
                </StackPanel>
                <Button Grid.Column="1" Content="{markup:Tr Common_Refresh}" Style="{StaticResource GhostButtonStyle}"
                        Command="{Binding RefreshCommand}" VerticalAlignment="Center" />
            </Grid>

            <ItemsControl ItemsSource="{Binding Gpus}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:HostGpu}">
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextPrimaryBrush}" />
                                        <Border CornerRadius="10" Padding="10,3" Margin="12,0,0,0" VerticalAlignment="Center"
                                                Background="{StaticResource SurfaceElevatedBrush}">
                                            <TextBlock FontSize="11" Foreground="{StaticResource SuccessBrush}">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value="{markup:Tr Gpu_PartitionSupported}" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding PartitionSupported}" Value="False">
                                                                <Setter Property="Text" Value="{markup:Tr Gpu_PartitionNotSupported}" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Margin="0,8,0,0" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}">
                                        <Run Text="VRAM :" /><Run Text="{Binding VramGb, Mode=OneWay}" /><Run Text="{markup:Tr Common_GbUnit}" />
                                        <Run Text="   -   " /><Run Text="{markup:Tr Gpu_Driver}" /><Run Text="{Binding DriverVersion}" />
                                    </TextBlock>
                                    <TextBlock Margin="0,8,0,0" FontSize="12" Foreground="{StaticResource TextMutedBrush}">
                                        <Run Text="{markup:Tr Gpu_VmsUsingThisGpu}" /><Run Text="{Binding AssignedVmNames.Count, Mode=OneWay}" />
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="{markup:Tr Gpu_NoGpuDetected}" Foreground="{StaticResource TextMutedBrush}"
                       Visibility="{Binding Gpus.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Invert}" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
