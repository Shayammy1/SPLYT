<UserControl x:Class="NovaVM.Gui.Controls.LanguageChoiceDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:NovaVM.Gui.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:LanguageChoiceDialogViewModel}">
    <Grid>
        <Border Style="{StaticResource CardStyle}" Width="380"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <!-- Textes volontairement bilingues et NON traduits : la langue de
                     l'interface est justement ce que cet ecran sert a choisir. -->
                <TextBlock Text="Choose your language" FontSize="18" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}" />
                <TextBlock Text="Choisissez votre langue" FontSize="14" Margin="0,2,0,18"
                           Foreground="{StaticResource TextSecondaryBrush}" />

                <ComboBox ItemsSource="{Binding AvailableLanguages}" SelectedItem="{Binding SelectedLanguage}"
                          Margin="0,0,0,20">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource AppLanguageToLabelConverter}}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <Button Content="OK" Style="{StaticResource AccentButtonStyle}"
                        Command="{Binding ConfirmCommand}" HorizontalAlignment="Right" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
