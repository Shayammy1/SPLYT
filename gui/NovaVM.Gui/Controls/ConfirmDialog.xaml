<UserControl x:Class="NovaVM.Gui.Controls.ConfirmDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:NovaVM.Gui.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:ConfirmDialogViewModel}">
    <Grid>
        <Border Style="{StaticResource CardStyle}" Width="460"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" TextWrapping="Wrap"
                           Margin="0,0,0,10" Foreground="{StaticResource TextPrimaryBrush}" />

                <TextBlock Text="{Binding Message}" TextWrapping="Wrap" FontSize="12" Margin="0,0,0,20"
                           Foreground="{StaticResource TextSecondaryBrush}" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{Binding CancelLabel}" Style="{StaticResource GhostButtonStyle}"
                            Command="{Binding CancelCommand}" Margin="0,0,10,0" />
                    <Button Content="{Binding SecondaryLabel}" Style="{StaticResource GhostButtonStyle}"
                            Command="{Binding SecondaryCommand}" Margin="0,0,10,0"
                            Visibility="{Binding HasSecondary, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <Button Content="{Binding PrimaryLabel}" Style="{StaticResource AccentButtonStyle}"
                            Command="{Binding PrimaryCommand}"
                            Visibility="{Binding PrimaryIsAccent, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <Button Content="{Binding PrimaryLabel}" Style="{StaticResource DangerButtonStyle}"
                            Command="{Binding PrimaryCommand}"
                            Visibility="{Binding PrimaryIsDanger, Converter={StaticResource BoolToVisibilityConverter}}" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
